# Claude Code
.claude/settings.local.json
.claude/.star-asked

# OMC (排除发布)
.omc/

# 密钥/环境变量 (R16.5 安全 P0)
.env
.env.*
!.env.example
*.pem
*.key
secrets/
credentials/

# PII 三重防御 (R23, 07-SECURITY_SECRETS.md) — 第 1·2 层: 关键词 + 专用文件夹。
# 第 3 层(文档扩展名 catch-all)因领域不同可能与产物重叠,故以注释提供 —
# 处理 PII 文档的工作区(合同、身份证等)务必取消注释或收窄路径后再应用。
*身份证*
*身份证号*
*户口*
*银行卡*
*银行账户*
*存折*
*个人信息*
pii/
private/
# *.pdf
# *.hwp
# *.hwpx
# *.docx
# *.xlsx

# OS
.DS_Store
Thumbs.db

# IDE
.idea/
.vscode/
*.swp

# 临时文件
*.tmp
*.temp
