# llm-wiki Changelog (정본)

> 이 워크스페이스 변경이력의 단일 정본. CLAUDE.md `## 변경 이력`에는 최근 3행만 유지하고
> 전체 이력은 여기에 누적한다. 핵심 메타파일(CLAUDE.md, 커맨드, 룰) 수정 시 한 줄 추가.

| 날짜 | 변경 내용 | 대상 | 사유 |
|------|----------|------|------|
| 2026-06-19 | GitHub 배포 — README 배포용 재작성(4동사·라우터 + `@gptaku_ai` Threads 브랜드) + LICENSE(MIT) + SessionStart **star opt-in 훅**(첫 세션 1회, gh 동의 시에만 `fivetaku/llm-wiki` star, `.star-asked` 마커) + .gitignore(`.star-asked`·`.omc`). `fivetaku/llm-wiki` public push(24파일, 민감정보 0) | README.md, LICENSE, .claude/hooks/session-start.sh, .gitignore | Threads 공유용 템플릿 배포. star는 opt-in(자동 fail-silent 거부) |
| 2026-06-19 | **라우팅/인덱스 대개편 (P0~P2 전량)** — 강의용 llm-wiki(Downloads) 비교 분석 반영. (1) `index.md`를 카탈로그→**라우터(MOC)**로 재정의(의도→타입/샤드, 엔티티 줄은 index에서 제거). (2) **타입별 하위 인덱스 `indexes/{type}.md` + 정본명 첫 글자 샤딩(≤50K)** 성장 경로 내재. (3) **`aliases.md` 정본화 레이어**(표기→정본명=샤드 키). (4) **query 2단 라우팅(Phase A Route/Phase B Search)** — 라우터+aliases만 읽고 샤드 결정, 샤드만 펼침, 넓히기 사다리. (5) **ingest(저장만)↔compile(위키화+raw 이동) 4동사 분리** — inbox 모델과 결합. (6) overview.md 종합, lazy 생성+tier(reviewed/auto)+auto-generated 대장, 동명 경로 명시 링크, provenance 4등급, 소크라테스 게이트, lint 라우터/샤드 정합 검사, .rag 선택적 파생 인프라 문서화. conventions.md 14절 전면 재작성 + CLAUDE.md(정체성·원칙·폴더트리·워크플로우·커맨드·Scale·트리거·프레임워크·산출물·품질) + 4커맨드(ingest/compile/query/lint) + 템플릿 3종 frontmatter(canonical/summary/tier/provenance) + structure.json + session-start 훅 + inbox/raw README 갱신. 검증 A 100%(47/47), 옛 경로 잔존 0 | conventions.md, CLAUDE.md, .claude/commands/{ingest,compile,query,lint}.md, 40-templates/*, 30-wiki/index.md, _meta/structure.json, .claude/hooks/session-start.sh, 10-inbox/20-raw README | 사용자 요청: 강의용 버전 분석 → "인덱스 구성이 LLM wiki의 핵심, 라우팅 개념을 내재해놔야 그에 맞춰 찾아간다". 빈 워크스페이스라 실물 샤드 대신 라우팅 메커니즘을 설계로 박아 첫 compile부터 그 구조로 성장하게 함 |
| 2026-06-19 | SessionStart 훅 신설 — `.claude/hooks/session-start.sh`(순수 bash, 외부 의존 0) + `.claude/settings.json` 등록. 위키가 비면(첫 사용) 온보딩 안내(데이터 넣는 법), 데이터가 있으면 현황(콘텐츠 페이지 수·inbox 미처리 수) + inbox 대기열 파일명 목록을 세션 컨텍스트로 주입(비차단). 두 분기 실행 테스트 통과. `.gitignore`는 settings.local.json만 무시 → settings.json·훅 추적됨(복사 시 자동 동작) | .claude/hooks/session-start.sh, .claude/settings.json | 사용자 요청: 아무 세팅 없이 워크스페이스에서 claude 실행 시 사용법이 자동으로 떠야 함. 빈 상태 온보딩 + 사용 중 현황을 한 훅으로 |
| 2026-06-19 | 10-inbox 진입 레이어 신설(미처리 소스 대기열) + 폴더 한 칸씩 뒤로(10-raw→20-raw, 20-wiki→30-wiki, 30-templates→40-templates, 40-queries→50-queries). 모든 활성 파일 경로 참조 일괄 치환. ingest 커맨드에 Step 6(처리 후 원본 inbox→raw mv) + Step 1 inbox 스캔 + source_file 최종경로(20-raw) 기록 명시. CLAUDE.md 폴더트리·워크플로우·Phase·정체성, 20-raw README 역할 재정의(처리완료 보관소), 10-inbox README 신설(데이터 넣는 법), structure.json folders 갱신 | 폴더 구조, CLAUDE.md, .claude/commands/ingest.md, 00-system/conventions.md, 10-inbox/README.md, 20-raw/README.md, _meta/structure.json | 받은 편지함(흐름=대기열) vs 영구 보관소(저장=불변 raw)의 역할 분리. 기존엔 10-raw가 둘을 겸해 "미처리 vs 처리완료" 추적 불가 → inbox에 남은 = 미처리, raw로 이동한 = 처리완료로 가시화 |
| 2026-06-14 | knowledge-base Custom Path → LLM-wiki 모델 재구성: 폴더(10-schema/20-data/30-relations/50-exports/frameworks 제거 → 00-system/10-raw/20-wiki/30-templates 신설), 커맨드(specify/implement/validate → ingest/query/lint), CLAUDE.md를 위키 운영 schema로 재작성, conventions.md·페이지 템플릿 3종·index/log 시드 추가 | 전체 | Karpathy "LLM Wiki" 패턴 구현 (raw/wiki/schema 3-layer), OMC 무의존 자족 설계 |
| 2026-06-14 | 워크스페이스 생성 (knowledge-base v1.3.0) | 전체 | workspace-builder generator |
