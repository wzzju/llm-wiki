# llm-wiki Changelog (正本)

> 本工作区变更历史的唯一正本。CLAUDE.md `## 变更历史` 中只保留最近 3 行，
> 全部历史在此累积。修改核心元文件（CLAUDE.md、命令、规则）时添加一行。

| 日期 | 变更内容 | 对象 | 事由 |
|------|----------|------|------|
| 2026-06-19 | GitHub 发布 — README 发布用重写（四动词·路由器 + `@gptaku_ai` Threads 品牌）+ LICENSE(MIT) + SessionStart **star opt-in 钩子**（首次会话 1 次，仅在 gh 同意时 star `fivetaku/llm-wiki`，`.star-asked` 标记）+ .gitignore(`.star-asked`·`.omc`)。`fivetaku/llm-wiki` public push（24 文件，敏感信息 0） | README.md, LICENSE, .claude/hooks/session-start.sh, .gitignore | Threads 共享用模板发布。star 为 opt-in（自动 fail-silent 拒绝） |
| 2026-06-19 | **路由/索引大改版（P0~P2 全量）** — 反映教学用 llm-wiki(Downloads) 对比分析。(1) 将 `index.md` 由目录→**路由器(MOC)** 重新定义（意图→类型/分片，实体行从 index 移除）。(2) 内化 **按类型的子索引 `indexes/{type}.md` + 规范名首字母分片(≤50K)** 成长路径。(3) **`aliases.md` 规范化层**（写法→规范名=分片键）。(4) **query 两段路由(Phase A Route/Phase B Search)** — 只读路由器+aliases 决定分片，只展开分片，扩展阶梯。(5) **ingest(仅保存)↔compile(维基化+raw 移动) 四动词分离** — 与 inbox 模型结合。(6) overview.md 综合，lazy 生成+tier(reviewed/auto)+auto-generated 总账，同名路径显式链接，provenance 四等级，苏格拉底门控，lint 路由器/分片一致性检查，.rag 可选派生基础设施文档化。conventions.md 14 节全面重写 + CLAUDE.md（身份·原则·文件夹树·工作流·命令·Scale·触发·框架·产出物·质量）+ 4 命令(ingest/compile/query/lint) + 模板 3 种 frontmatter(canonical/summary/tier/provenance) + structure.json + session-start 钩子 + inbox/raw README 更新。验证 A 100%(47/47)，旧路径残留 0 | conventions.md, CLAUDE.md, .claude/commands/{ingest,compile,query,lint}.md, 40-templates/*, 30-wiki/index.md, _meta/structure.json, .claude/hooks/session-start.sh, 10-inbox/20-raw README | 用户请求：教学用版本分析 → "索引构成是 LLM wiki 的核心，必须内化路由概念才能据此找到目标"。因是空工作区，故不用实际分片而将路由机制固化为设计，使首次 compile 起就以该结构成长 |
| 2026-06-19 | SessionStart 钩子新建 — `.claude/hooks/session-start.sh`（纯 bash，外部依赖 0）+ `.claude/settings.json` 注册。维基为空时（首次使用）引导提示（放入数据的方法），有数据时将现状（内容页面数·inbox 未处理数）+ inbox 队列文件名列表作为会话上下文注入（非阻塞）。两分支执行测试通过。`.gitignore` 仅忽略 settings.local.json → settings.json·钩子被跟踪（复制时自动生效） | .claude/hooks/session-start.sh, .claude/settings.json | 用户请求：在无任何配置的工作区中执行 claude 时应自动弹出用法。空状态引导 + 使用中现状用一个钩子实现 |
| 2026-06-19 | 10-inbox 入口层新建（未处理源队列）+ 文件夹各向后挪一格（10-raw→20-raw, 20-wiki→30-wiki, 30-templates→40-templates, 40-queries→50-queries）。所有活动文件路径引用一并替换。ingest 命令新增 Step 6（处理后将原始文件 inbox→raw mv）+ Step 1 inbox 扫描 + source_file 记录最终路径(20-raw)。CLAUDE.md 文件夹树·工作流·Phase·身份，20-raw README 角色重新定义（处理完成保管库），10-inbox README 新建（放入数据的方法），structure.json folders 更新 | 文件夹结构, CLAUDE.md, .claude/commands/ingest.md, 00-system/conventions.md, 10-inbox/README.md, 20-raw/README.md, _meta/structure.json | 收件箱（流=队列）vs 永久保管库（保存=不可变 raw）的角色分离。此前 10-raw 兼任两者，无法跟踪"未处理 vs 处理完成" → 留在 inbox 的=未处理，移至 raw 的=处理完成，由此可视化 |
| 2026-06-14 | knowledge-base Custom Path → LLM-wiki 模型重构：文件夹（移除 10-schema/20-data/30-relations/50-exports/frameworks → 新建 00-system/10-raw/20-wiki/30-templates），命令（specify/implement/validate → ingest/query/lint），将 CLAUDE.md 重写为维基运营 schema，新增 conventions.md·页面模板 3 种·index/log 种子 | 全部 | 实现 Karpathy "LLM Wiki" 模式（raw/wiki/schema 3-layer），OMC 无依赖自足设计 |
| 2026-06-14 | 创建工作区 (knowledge-base v1.3.0) | 全部 | workspace-builder generator |
